# AI Study Companion - Backend

This is the backend for the AI Study Companion application.

## Getting Started

1. Copy `.env.example` to `.env`
2. Run `npm install`
3. Run `npm run dev` to start the development server
